# Test Specifications: Trust-Lean v1.2.0

Generated: 2026-02-21T23:35:48Z
Project: /Users/manuelpuebla/Documents/claudio/Trust-Lean
Toolchain: leanprover/lean4:v4.26.0
Mathlib: yes

> **Este archivo es leído por otra sesión de Claude Code que escribe
> los archivos .lean de test. NO contiene código compilable.**

## Instrucciones para la sesión de testing

1. Leer este archivo completo
2. Para cada nodo, leer el código fuente real (`scout.py` + `Read`)
3. Escribir `Tests/Properties/{NodeName}.lean` con las propiedades especificadas
4. Escribir `Tests/Integration/{NodeName}.lean` con los integration tests
5. Compilar cada archivo con `lake env lean Tests/.../*.lean` hasta que pase
6. Usar `/ask-lean` o `/ask-dojo` si faltan tácticas o instancias

### Convenciones obligatorias

**Properties** (`Tests/Properties/{Name}.lean`):
- `import Mathlib.Testing.SlimCheck` (si Mathlib disponible)
- Cada propiedad como `example` o `theorem` con `slim_check` tactic
- Comentario con prioridad: `-- P0, INVARIANT: descripción`
- Si un tipo necesita `SampleableExt` y no existe: `-- NOT_YET_RUNNABLE`

**Integration** (`Tests/Integration/{Name}.lean`):
- Cada test imprime `[PASS] nombre` o `[FAIL] nombre`
- Función `main : IO UInt32` que retorna 0 si todo pasa, 1 si hay fallos
- Pattern: `def T1_name : IO Bool := do ...`

### Ejecución (la hace la sesión implementadora)

```bash
# La sesión implementadora ejecuta:
python3 ~/.claude/skills/plan-project/scripts/close_block.py \
  --project PATH --block "Bloque N" --nodes '{...}'
# Que internamente corre: lake env lean Tests/Properties/*.lean
#                         lake env lean Tests/Integration/*.lean
```

## Criterios de rúbrica (de BENCHMARKS.md)


Rubric generated by `/benchmark-qa --strict` during planning.
This section is written ONCE and not modified during execution.

### Correctness
- sanitizeIdentifier output is never a C keyword
- sanitizeIdentifier output is a valid C identifier
- sanitizeIdentifier is idempotent
- stmtToC produces balanced braces
- stmtToC is deterministic (pure function)
- All 12 Stmt constructors handled
- store/load base correctly parenthesized
- gcc -Wall -Werror compiles output

### Performance
- Total LOC < 6000
- Build time < 6s
- Verified statements >= 330
- Sorry count = 0

### Quality
- No simp[*] or native_decide
- All public defs have doc comments
- No regressions in v1.0.0/v1.1.0 functionality
- Headers autocontenidos (stdint.h, stdbool.h)


---

## Especificaciones por nodo

### N1.1 — Core Value + Stmt

- **Tipo**: FUNDACIONAL
- **Archivos fuente**: `TrustLean/Core/Value.lean`, `TrustLean/Core/Stmt.lean`
- **Target properties**: `Tests/Properties/CoreValue+Stmt.lean`
- **Target integration**: `Tests/Integration/CoreValue+Stmt.lean`
- **Properties**: 12 (4 P0)
- **Integration tests**: 10

PROPERTIES:
- [P1] P0 SOUNDNESS: `evalBinOp` on integer types correctly reflects the behavior of the underlying integer operations in Lean.
  Sketch: example (op : BinOp) (n m : Int) (h : op.isIntOp) : evalBinOp op (.int n) (.int m) = (match op with | .add => some (n+m) | .sub => some (n-m) | .mul => some (n*m) | .div => if m=0 then none else some (n/m) | .mod => if m=0 then none else some (n%m) | _ => none) >>= (some ∘ .int)
  SampleableExt: yes
  Risk: Incorrect arithmetic calculations in the target language. This is a fundamental correctness property.

- [P2] P0 SOUNDNESS: `evalBinOp` on boolean types correctly reflects the behavior of the underlying boolean operations in Lean.
  Sketch: example (op : BinOp) (a b : Bool) (h : op.isBoolOp) : evalBinOp op (.bool a) (.bool b) = (match op with | .and => some (a && b) | .or => some (a || b) | _ => none) >>= (some ∘ .bool)
  SampleableExt: yes
  Risk: Incorrect logical operations in the target language.

- [P3] P1 COMMUTATIVITY: `evalBinOp` is commutative for operations like add, mul, and, or, eq, ne.
  Sketch: example (op : BinOp) (v1 v2 : Value) (h : op ∈ [BinOp.add, .mul, .and, .or, .eq, .ne]) : evalBinOp op v1 v2 = evalBinOp op v2 v1
  SampleableExt: yes
  Risk: Optimizations based on commutativity would be invalid. Code may produce different results depending on expression order.

- [P4] P1 INVARIANT: `evalBinOp` on mismatched value types (e.g., int and bool) always returns `none`.
  Sketch: example (op : BinOp) (n : Int) (b : Bool) : evalBinOp op (.int n) (.bool b) = none ∧ evalBinOp op (.bool b) (.int n) = none
  SampleableExt: yes
  Risk: Type confusion and undefined behavior in the compiled program if not caught.

- [P5] P1 SOUNDNESS: `evalUnaryOp` correctly reflects the behavior of the underlying operations (`not` for bool, `neg` for int).
  Sketch: example (op : UnaryOp) (v : Value) : evalUnaryOp op v = match op, v with | .not, .bool b => some (.bool (!b)) | .neg, .int i => some (.int (-i)) | _, _ => none
  SampleableExt: yes
  Risk: Incorrect unary calculations (negation, logical not).

- [P6] P1 IDEMPOTENCY: Applying `not` or `neg` twice is an identity operation (involution).
  Sketch: example (op : UnaryOp) (v : Value) : evalUnaryOp op v >>= evalUnaryOp op = if (op,v) matches (.not, .bool _) ∨ (op, .int _) then some v else none
  SampleableExt: yes
  Risk: Logic errors, e.g., `!!x` not being equivalent to `x`.

- [P7] P0 INVARIANT: The read-over-write property holds for `LowLevelEnv.update`. Updating a key and then reading it yields the new value.
  Sketch: example (env : LowLevelEnv) (k : VarName) (v : Value) : (env.update k v).find? k = some v
  SampleableExt: yes
  Risk: Variable assignment and state management would be fundamentally broken.

- [P8] P1 PRESERVATION: Updating a key in `LowLevelEnv` does not affect other keys.
  Sketch: example (env : LowLevelEnv) (k1 k2 : VarName) (v : Value) (h : k1 ≠ k2) : (env.update k1 v).find? k2 = env.find? k2
  SampleableExt: yes
  Risk: Variable assignments could have unintended side effects, corrupting unrelated state.

- [P9] P0 INVARIANT: `CodeGenState.freshVar` produces a new variable name that is different from the one just produced.
  Sketch: example (s : CodeGenState) : let (v1, s') := s.freshVar; let (v2, _) := s'.freshVar; v1 ≠ v2
  SampleableExt: yes
  Risk: Variable name collision, leading to incorrect scoping and shadowing, producing invalid C code.

- [P10] P1 EQUIVALENCE: `Stmt.desugarFor` correctly translates a for-loop into an equivalent while-loop structure.
  Sketch: example (x : VarName) (start finish : LowLevelExpr) (body : Stmt) : Stmt.desugarFor x start finish body = Stmt.scope (Stmt.seq (Stmt.decl x start) (Stmt.while (LowLevelExpr.binOp .lt (LowLevelExpr.var x) finish) (Stmt.seq body (Stmt.assign x (LowLevelExpr.binOp .add (LowLevelExpr.var x) (LowLevelExpr.val (.int 1)))))))
  SampleableExt: yes
  Risk: For-loops, a fundamental control flow structure, would be implemented incorrectly.

- [P11] P2 INVARIANT: The boolean predicates on `Outcome` are mutually exclusive where logically required.
  Sketch: example (o : Outcome) : ¬ (o.isNormal ∧ o.isLoopSignal) ∧ ¬ (o.isNormal ∧ o.isTerminating)
  SampleableExt: yes
  Risk: Internal logic for control flow handling during statement execution could fail, leading to incorrect program behavior.

- [P12] P1 SOUNDNESS: `assignmentsToStmt` correctly folds a list of assignments into a nested `Stmt.seq`.
  Sketch: theorem assignmentsToStmt_correct (as : List Assignment) : assignmentsToStmt as = as.foldr (fun a s => Stmt.seq (Stmt.assign a.lhs a.rhs) s) Stmt.noop
  SampleableExt: yes
  Risk: Code generation for sequences of assignments could be malformed.

INTEGRATION:
- [T1] BASIC: `evalBinOp` performs correct addition and multiplication on integer values.
  Setup: `evalBinOp .add (.int 5) (.int 3)` and `evalBinOp .mul (.int 4) (.int 2)`
  Check: Results should be `some (.int 8)` and `some (.int 8)` respectively.

- [T2] EDGE_CASE: `evalBinOp` handles division by zero for both `.div` and `.mod`.
  Setup: `evalBinOp .div (.int 10) (.int 0)` and `evalBinOp .mod (.int 10) (.int 0)`
  Check: Both results should be `none`.

- [T3] BASIC: `evalBinOp` performs correct logical operations on boolean values.
  Setup: `evalBinOp .and (.bool true) (.bool false)` and `evalBinOp .or (.bool true) (.bool false)`
  Check: Results should be `some (.bool false)` and `some (.bool true)` respectively.

- [T4] EDGE_CASE: `evalBinOp` returns `none` when types are mismatched for an arithmetic operation.
  Setup: `evalBinOp .add (.int 5) (.bool true)`
  Check: Result should be `none`.

- [T5] BASIC: `evalUnaryOp` correctly negates an integer and a boolean.
  Setup: `evalUnaryOp .neg (.int 10)` and `evalUnaryOp .not (.bool false)`
  Check: Results should be `some (.int (-10))` and `some (.bool true)` respectively.

- [T6] BASIC: `LowLevelEnv` update and lookup works as expected.
  Setup: `let v_x := VarName.mk "x"; let v_y := VarName.mk "y"; let env0 := LowLevelEnv.default; let env1 := env0.update v_x (.int 42); let env2 := env1.update v_y (.bool false)`
  Check: `env2.find? v_x` should be `some (.int 42)`. `env2.find? v_y` should be `some (.bool false)`. `env2.find? (VarName.mk "z")` should be `none`.

- [T7] BASIC: `Stmt.desugarFor` produces the correct structure for a simple loop.
  Setup: `let body := Stmt.expr (LowLevelExpr.var (VarName.mk "i")); Stmt.desugarFor (VarName.mk "i") (LowLevelExpr.val (.int 0)) (LowLevelExpr.val (.int 3)) body`
  Check: The resulting `Stmt` should have the structure `scope(decl i = 0; while(i < 3){ expr i; i = i + 1; })`.

- [T8] EDGE_CASE: `Stmt.desugarFor` handles a zero-iteration loop correctly.
  Setup: `let body := Stmt.expr (LowLevelExpr.var (VarName.mk "i")); Stmt.desugarFor (VarName.mk "i") (LowLevelExpr.val (.int 5)) (LowLevelExpr.val (.int 5)) body`
  Check: The resulting `Stmt` should have a `while` condition that is immediately false (e.g., `5 < 5`).

- [T9] EDGE_CASE: `assignmentsToStmt` handles an empty list of assignments.
  Setup: `assignmentsToStmt []`
  Check: Result should be `Stmt.noop`.

- [T10] BASIC: `assignmentsToStmt` handles a multi-element list.
  Setup: `let a1 := { lhs := .mk "x", rhs := .val (.int 1) }; let a2 := { lhs := .mk "y", rhs := .val (.int 2) }; assignmentsToStmt [a1, a2]`
  Check: Result should be `Stmt.seq (Stmt.assign a1.lhs a1.rhs) (Stmt.assign a2.lhs a2.rhs)`.

---

### N1.2 — Core Eval + Foundation

- **Tipo**: FUNDACIONAL
- **Archivos fuente**: `TrustLean/Core/Eval.lean`, `TrustLean/Core/Foundation.lean`, `TrustLean/Core/FuelMono.lean`
- **Target properties**: `Tests/Properties/CoreEval+Foundation.lean`
- **Target integration**: `Tests/Integration/CoreEval+Foundation.lean`
- **Properties**: 5 (2 P0)
- **Integration tests**: 10

PROPERTIES:
- [P1] P0 INVARIANT: `evalExpr` does not modify the state.
  Sketch: example (e : Expr) (st : State) : (evalExpr e st).2 = st := by slim_check
  SampleableExt: yes
  Risk: Bugs where reading a variable or computing an expression has unintended side-effects on the state, violating referential transparency.

- [P2] P0 PRESERVATION: `evalStmt` is monotonic with respect to fuel.
  Sketch: example (s : Stmt) (st : State) (n : Nat) (h : (evalStmt s {st with fuel := n}).isOk) : evalStmt s {st with fuel := n} = evalStmt s {st with fuel := n + 1} := by slim_check
  SampleableExt: yes
  Risk: The fuel mechanism is flawed, leading to non-deterministic results based on the initial fuel provided, or breaking termination guarantees.

- [P3] P1 SOUNDNESS: `break` propagation through sequential composition.
  Sketch: example (s : Stmt) (st : State) : (evalStmt (Stmt.seq Stmt.break s) st).isBreak := by slim_check
  SampleableExt: yes
  Risk: Incorrect control flow. `break` statements might fail to exit their context, causing loops to execute incorrectly or not terminate.

- [P4] P1 INVARIANT: Frame property for `evalStmt`.
  Description: Evaluating a statement `s` does not alter the value of variables that are not syntactically assigned to within `s`.
  Sketch: example (s : Stmt) (x : Var) (st : State) (h : x ∉ assignedVars s) : (evalStmt s st).state.env.lookup x = st.env.lookup x := by slim_check
  SampleableExt: yes
  Risk: Action-at-a-distance bugs where statements have unexpected side effects on unrelated parts of the program state.

- [P5] P2 EQUIVALENCE: `skip` is the identity for sequential execution.
  Description: Evaluating `skip` is a no-op, and composing it with any statement `s` is equivalent to evaluating `s` alone.
  Sketch: example (s : Stmt) (st : State) : evalStmt (Stmt.seq s Stmt.skip) st = evalStmt s st := by slim_check
  SampleableExt: yes
  Risk: Incorrect program semantics, especially in contexts of automated program transformation or simplification where `skip` statements might be added or removed.

INTEGRATION:
- [T1] BASIC: Simple variable assignment.
  Setup: Construct a state with `env := {"x" := 0}` and `fuel := 100`. Evaluate the statement `x := 42`.
  Check: The final environment maps "x" to the value 42.

- [T2] BASIC: `if-then-else` executes the `then` branch on a true condition.
  Setup: Construct a state with `env := {"y" := 0}`. Evaluate `if true then y := 1 else y := 2`.
  Check: The final environment maps "y" to the value 1.

- [T3] BASIC: `if-then-else` executes the `else` branch on a false condition.
  Setup: Construct a state with `env := {"y" := 0}`. Evaluate `if false then y := 1 else y := 2`.
  Check: The final environment maps "y" to the value 2.

- [T4] BASIC: A simple `while` loop terminates correctly.
  Setup: Construct a state with `env := {"i" := 3}` and `fuel := 100`. Evaluate the program `while i > 0 do i := i - 1`.
  Check: The final environment maps "i" to the value 0.

- [T5] EDGE_CASE: A `while` loop with a `break` statement exits correctly.
  Setup: Construct a state with `env := {"i" := 10}` and `fuel := 100`. Evaluate `while true do if i == 5 then break; i := i - 1`.
  Check: The final environment maps "i" to the value 5, and the evaluation terminates with a normal status (not `break`).

- [T6] EDGE_CASE: A `while` loop with a `continue` statement skips the remainder of the body.
  Setup: Construct a program to sum odd numbers from 9 down to 1, using `continue` to skip even numbers. Initial state: `env := {"i" := 10, "sum" := 0}`, `fuel := 200`.
  Check: The final environment maps "sum" to 25 (i.e., 9+7+5+3+1).

- [T7] EDGE_CASE: A `while` loop with an initially false condition executes zero times.
  Setup: Construct a state with `env := {"x" := 99}`. Evaluate `while false do x := 0`.
  Check: The final environment still maps "x" to 99. The state is unchanged.

- [T8] EDGE_CASE: Evaluation halts correctly when fuel is exhausted.
  Setup: Construct a state with `env := {"i" := 10}` and `fuel := 5`. Evaluate `while i > 0 do i := i - 1`.
  Check: The evaluation result is `outOfFuel`. The final value of "i" should be greater than 0, reflecting partial execution.

- [T9] STRESS: Nested `while` loops execute correctly.
  Setup: Construct a program with nested loops to calculate a sum. Outer loop: `i` from 1 to 10. Inner loop: `j` from 0 to `i-1`. Increment a `total` counter in the inner loop. Initial state: `env := {"i":=0, "j":=0, "total":=0}`, `fuel := 1000`.
  Check: The final environment maps "total" to 45 (the sum of 0+1+2+...+9).

- [T10] REGRESSION: `return` propagates out of all control flow constructs.
  Setup: Evaluate `while true do if true then return 42`.
  Check: The final result of the evaluation is `return(42)`, not `normal` or `break`.

---

### N1.3 — Typeclasses

- **Tipo**: CRITICO
- **Archivos fuente**: `TrustLean/Typeclass/CodeGenerable.lean`, `TrustLean/Typeclass/CodeGenSound.lean`, `TrustLean/Typeclass/BackendEmitter.lean`
- **Target properties**: `Tests/Properties/Typeclasses.lean`
- **Target integration**: `Tests/Integration/Typeclasses.lean`
- **Properties**: 5 (4 P0)
- **Integration tests**: 6

PROPERTIES:
- [P1] P0 INVARIANT: The `toCode` function in any `CodeGenerable` instance must be deterministic.
  Sketch: example [CodeGenerable α] (x : α) : toCode x = toCode x := by slim_check
  SampleableExt: yes
  Risk: Non-deterministic code generation can lead to unreproducible compiler errors and heisenbugs in the generated artifacts, making debugging impossible.

- [P2] P0 SOUNDNESS: Any instance of `CodeGenSound` must satisfy its `toCode_sound` property.
  Sketch: example [CodeGenSound α] (x : α) (env : Env) (fuel : Nat) : evalStmt fuel (Stmt.seqList (toCode x)) env = .normal (eval x env) := by slim_check
  SampleableExt: yes
  Risk: Critical. A failure here means the code generator is fundamentally broken, producing code that does not match the semantics of the source program. This is the primary correctness theorem for the compiler.

- [P3] P1 IDEMPOTENCY: The C backend's identifier sanitization function must be idempotent.
  Sketch: example (s : String) : sanitizeIdentifier (sanitizeIdentifier s) = sanitizeIdentifier s := by slim_check
  SampleableExt: yes
  Risk: If sanitization is not idempotent, applying it multiple times (e.g., in different compiler passes) could lead to multiply-mangled names (e.g., `_int` -> `__int`) and broken variable references.

- [P4] P0 INVARIANT: The C backend's `sanitizeIdentifier` function must produce a valid C identifier that is not a C keyword.
  Sketch: example (s : String) : isValidCIdentifier (sanitizeIdentifier s) ∧ ¬isCKeyword (sanitizeIdentifier s) := by slim_check
  SampleableExt: yes
  Risk: Generating invalid identifiers or C keywords as variable names will cause the C compiler to fail with a syntax error.

- [P5] P0 INVARIANT: The C code generated for any `Stmt` must have balanced curly braces.
  Sketch: example (s : Stmt) : bracesAreBalanced ([BackendEmitter Stmt].emit s) := by slim_check
  SampleableExt: yes
  Risk: Unbalanced braces are a fundamental syntax error that will cause C compilation to fail.

INTEGRATION:
- [T1] BASIC: End-to-end code generation for a simple sequence of assignments.
  Setup: Construct a `Stmt` for the program `x := 10; y := x + 5;`.
  Check: The `#eval` of `[BackendEmitter Stmt].emit s` should produce a valid C code block containing the substrings `int64_t x = 10;` and `int64_t y = x + 5;`.

- [T2] EDGE_CASE: Code generation for a no-op statement.
  Setup: `let s := Stmt.skip`
  Check: The emitted C code should be a valid, semantically equivalent no-op, such as a single semicolon `;` or an empty block `{}`. The test should check for this exact expected output.

- [T3] BASIC: Code generation for nested control flow structures.
  Setup: Construct a `Stmt` for a `while` loop containing an `if-then-else` block.
  Check: The emitted C code must be correctly indented and have properly nested, balanced curly braces for both the `while` and `if` blocks.

- [T4] EDGE_CASE: Identifier sanitization for C keywords and invalid characters.
  Setup: Construct a `Stmt` that assigns to variables named `for`, `continue`, and `my-variable`.
  Check: The emitted C code must use sanitized names, for example `_for`, `_continue`, and `my_variable`.

- [T5] BASIC: Coverage of all `Stmt` constructors.
  Setup: Create a list of `Stmt`, with each element exercising a different `Stmt` constructor (e.g., `assign`, `store`, `load`, `if`, `while`, `call`, etc.).
  Check: The `emit` function should not panic or fail. The resulting C code string should be non-empty and appear well-formed, confirming all cases are handled.

- [T6] EDGE_CASE: Code generation for memory operations (`load`/`store`).
  Setup: Construct a `Stmt` sequence for `store(base, 42); x := load(base + 8);`.
  Check: The emitted C code must use correct C pointer syntax, including dereferencing (`*`) and pointer arithmetic. For example, `*base = 42;` and `int64_t x = *(base + 1);` (assuming `base` is `int64_t*`). This verifies the "store/load base correctly parenthesized" rubric item.

---

### N2.1 — ArithExpr Frontend

- **Tipo**: PARALELO
- **Archivos fuente**: `TrustLean/Frontend/ArithExpr/Syntax.lean`, `TrustLean/Frontend/ArithExpr/Compile.lean`, `TrustLean/Frontend/ArithExpr/Correctness.lean`
- **Target properties**: `Tests/Properties/ArithExprFrontend.lean`
- **Target integration**: `Tests/Integration/ArithExprFrontend.lean`
- **Properties**: 5 (1 P0)
- **Integration tests**: 8

PROPERTIES:
- [P1] P0 SOUNDNESS: The result of evaluating an `ArithExpr` directly must be equivalent to the result of running its compiled `Stmt` representation. This is the main correctness property for the compiler frontend.
  Sketch: example (e : ArithExpr) (ctx : Ctx) : (run (ArithExpr.compile e) ctx).get? "result" = some (ArithExpr.eval e ctx) := by slim_check
  SampleableExt: yes
  Risk: The compiler is fundamentally incorrect; the generated low-level code does not match the semantics of the high-level arithmetic expression.

- [P2] P1 INVARIANT: The `ArithExpr.eval` function must be deterministic. Given the same expression and context, it must always produce the same value.
  Sketch: example (e : ArithExpr) (ctx : Ctx) : ArithExpr.eval e ctx = ArithExpr.eval e ctx := by slim_check
  SampleableExt: yes
  Risk: The semantic model is unreliable, making proofs of correctness against it meaningless and potentially leading to non-reproducible behavior.

- [P3] P1 INVARIANT: The `ArithExpr.compile` function must be deterministic. Given the same expression, it must always produce the same `Stmt`.
  Sketch: example (e : ArithExpr) : ArithExpr.compile e = ArithExpr.compile e := by slim_check
  SampleableExt: yes
  Risk: The compilation process is unpredictable, which complicates debugging, formal verification, and could lead to inconsistent generated code.

- [P4] P1 EQUIVALENCE: Evaluating a constant expression `ArithExpr.const n` should yield the value `n`, regardless of the context.
  Sketch: example (n : Int) (ctx : Ctx) : ArithExpr.eval (ArithExpr.const n) ctx = Value.int n := by slim_check
  SampleableExt: yes
  Risk: The base case for evaluation is broken, causing all subsequent calculations involving constants to be incorrect.

- [P5] P2 PRESERVATION: The set of variables read by the compiled `Stmt` must be a subset of the variables present in the original `ArithExpr`. The compiler should not introduce dependencies on unrelated variables.
  Sketch: example (e : ArithExpr) : (ArithExpr.compile e).readVars ⊆ e.vars := by slim_check
  SampleableExt: yes
  Risk: Compiled code could have spurious dependencies on global state, leading to incorrect and unpredictable results when integrated into larger programs.

INTEGRATION:
- [T1] BASIC: A simple expression with addition of a constant and a variable.
  Setup: def expr := ArithExpr.add (ArithExpr.const 10) (ArithExpr.var "x"); def ctx := [("x", Value.int 5)]
  Check: `ArithExpr.eval expr ctx` should equal `Value.int 15`. The compiled and run statement should also produce `Value.int 15`.

- [T2] BASIC: A nested expression involving multiplication and subtraction to test evaluation order.
  Setup: def expr := ArithExpr.mul (ArithExpr.const 5) (ArithExpr.sub (ArithExpr.var "y") (ArithExpr.const 2)); def ctx := [("y", Value.int 10)]
  Check: `ArithExpr.eval expr ctx` should equal `Value.int 40` (i.e., 5 * (10 - 2)). The compiled version should match.

- [T3] EDGE_CASE: An expression involving the integer zero.
  Setup: def expr := ArithExpr.mul (ArithExpr.add (ArithExpr.var "a") (ArithExpr.const 100)) (ArithExpr.const 0); def ctx := [("a", Value.int 12345)]
  Check: `ArithExpr.eval expr ctx` should equal `Value.int 0`. The compiled version should match.

- [T4] EDGE_CASE: An expression involving negative numbers in multiple operations.
  Setup: def expr := ArithExpr.add (ArithExpr.mul (ArithExpr.const (-5)) (ArithExpr.var "x")) (ArithExpr.const (-10)); def ctx := [("x", Value.int 3)]
  Check: `ArithExpr.eval expr ctx` should equal `Value.int (-25)` (i.e., (-15) + (-10)). The compiled version should match.

- [T5] EDGE_CASE: An expression with a variable that is not present in the evaluation context.
  Setup: def expr := ArithExpr.add (ArithExpr.var "defined") (ArithExpr.var "undefined"); def ctx := [("defined", Value.int 10)]
  Check: The behavior for unbound variables must be consistent. Assuming it defaults to 0, `ArithExpr.eval expr ctx` should equal `Value.int 10`. The compiled version must exhibit the same default behavior.

- [T6] EDGE_CASE: An expression that is just a single variable.
  Setup: def expr := ArithExpr.var "x"; def ctx := [("x", Value.int 99)]
  Check: `ArithExpr.eval expr ctx` should equal `Value.int 99`. The compiled version should correctly load the variable's value.

- [T7] STRESS: A deeply-nested, right-associative expression to test for potential stack overflow in recursive functions (`eval`, `compile`).
  Setup: A programmatically generated expression of the form `add (const 1) (add (const 1) ...)` with a depth of 500.
  Check: Both `eval` and `compile` should complete without stack overflow errors. The evaluated result should be `Value.int 500`.

- [T8] STRESS: A wide expression tree involving many distinct variables to test performance with a large context.
  Setup: An expression that sums 100 unique variables: `add (var "v0") (add (var "v1") ...)`. A context mapping each `v_i` to `Value.int 1`.
  Check: Both `eval` and `compile` should complete in a reasonable time. The evaluated result should be `Value.int 100`.

---

### N2.2 — BoolExpr Frontend

- **Tipo**: PARALELO
- **Archivos fuente**: `TrustLean/Frontend/BoolExpr/Syntax.lean`, `TrustLean/Frontend/BoolExpr/Compile.lean`, `TrustLean/Frontend/BoolExpr/Correctness.lean`
- **Target properties**: `Tests/Properties/BoolExprFrontend.lean`
- **Target integration**: `Tests/Integration/BoolExprFrontend.lean`
- **Properties**: 3 (1 P0)
- **Integration tests**: 6

PROPERTIES:
- [P1] P0 SOUNDNESS: The evaluation of a compiled `BoolExpr` via the `Stmt` interpreter produces the same boolean result as the direct evaluation of the `BoolExpr`. This is the fundamental correctness property of the compiler.
  Sketch: example (e : BoolExpr) (env : Env) (fuel : Nat) (h : fuel > fuelBound e) : (evalStmt fuel (e.compile) env).get "result" >>= Value.getBool = some (e.eval env) := by slim_check
  SampleableExt: yes
  Risk: The compiler is incorrect, generating code that does not match the language's specified semantics. This would invalidate the entire backend for boolean expressions.

- [P2] P1 INVARIANT: The evaluation of a `BoolExpr` is invariant to changes in the environment for variables not present in the expression. This property verifies the correctness of the `boolVarNames` dependency analysis.
  Sketch: example (e : BoolExpr) (env1 env2 : Env) (h : ∀ v ∈ boolVarNames e, env1.lookup v = env2.lookup v) : e.eval env1 = e.eval env2 := by slim_check
  SampleableExt: yes
  Risk: Incorrect dependency analysis could lead to faulty program transformations, optimizations, or incorrect behavior in systems that rely on this function for variable scoping.

- [P3] P2 EQUIVALENCE: The `BoolExpr.eval` function respects fundamental boolean identities, such as double negation elimination (`¬¬p ↔ p`) and De Morgan's laws.
  Sketch: example (e : BoolExpr) (env : Env) : BoolExpr.eval (.not (.not e)) env = BoolExpr.eval e env := by slim_check
  SampleableExt: yes
  Risk: The semantic definition of logical operators in `BoolExpr.eval` could be subtly wrong (e.g., using bitwise instead of logical operations, or a copy-paste error), leading to incorrect results in complex expressions.

INTEGRATION:
- [T1] BASIC: Test evaluation of simple expressions for each logical and relational operator.
  Setup: `let env : Env := [("x", .int 10), ("y", .int 20)]`. Construct four expressions: `e1 := .lt (.var "x") (.const 20)`, `e2 := .eq (.var "y") (.const 10)`, `e3 := .and e1 (.not e2)`, `e4 := .or e2 (.const false)`.
  Check: `e1.eval env` is `true`, `e2.eval env` is `false`, `e3.eval env` is `true`, `e4.eval env` is `false`.

- [T2] EDGE_CASE: Test short-circuiting behavior for `and` and `or`.
  Setup: `let env : Env := []`. Construct `e_and := .and (.const false) (.eq (.var "y") (.const 0))` and `e_or := .or (.const true) (.eq (.var "y") (.const 0))`. Note that variable "y" is not in the environment.
  Check: `e_and.eval env` should evaluate to `false` without error. `e_or.eval env` should evaluate to `true` without error. This confirms that the undefined variable is not accessed.

- [T3] EDGE_CASE: Test behavior when a required variable is not found in the environment.
  Setup: `let env : Env := []`. Construct `e := .lt (.var "z") (.const 10)`.
  Check: The result should match the specified default behavior for missing variables (e.g., assuming a default value of 0). If variables default to 0, `e.eval env` should be `true`.

- [T4] BASIC: Test a complex expression with mixed operators to verify precedence and composition.
  Setup: `let env : Env := [("a", .int 5), ("b", .int 10), ("c", .int 5)]`. Construct `e := .or (.and (.lt (.var "a") (.var "b")) (.eq (.var "a") (.var "c"))) (.lt (.var "b") (.var "a"))`. This corresponds to `(5 < 10 && 5 == 5) || (10 < 5)`.
  Check: `e.eval env` should be `true`.

- [T5] EDGE_CASE: Test the `boolVarNames` helper function with zero and one variable.
  Setup: `let e_const := .and (.const true) (.lt (.const 1) (.const 2))`. `let e_single := .eq (.var "x") (.add (.var "x") (.const 1))`.
  Check: `boolVarNames e_const` should be an empty collection. `boolVarNames e_single` should be a collection containing exactly "x".

- [T6] STRESS: Test evaluation and compilation of a deeply nested expression to detect potential stack overflow errors.
  Setup: Programmatically construct a `BoolExpr` with 1000 nested `not` operators: `let e_deep := .not (.not (... (.not (.const true))...))`.
  Check: Both `e_deep.eval []` and `e_deep.compile` should complete without crashing or timing out. The result of evaluation should be `true`.

---

### N2.3 — ImpStmt Frontend

- **Tipo**: PARALELO
- **Archivos fuente**: `TrustLean/Frontend/ImpStmt/Syntax.lean`, `TrustLean/Frontend/ImpStmt/Compile.lean`, `TrustLean/Frontend/ImpStmt/Correctness.lean`
- **Target properties**: `Tests/Properties/ImpStmtFrontend.lean`
- **Target integration**: `Tests/Integration/ImpStmtFrontend.lean`
- **Properties**: 6 (3 P0)
- **Integration tests**: 11

PROPERTIES:
- [P1] P0 SOUNDNESS: The evaluation of an arithmetic expression `ImpExpr` is compositionally correct with respect to its constructors.
  Sketch: example (e1 e2 : ImpExpr) (env : ImpEnv) : (ImpExpr.add e1 e2).eval env = e1.eval env + e2.eval env := by slim_check
  SampleableExt: yes
  Risk: The arithmetic interpreter (`ImpExpr.eval`) could have fundamental bugs, leading to incorrect calculations in all programs.

- [P2] P0 SOUNDNESS: The evaluation of a boolean expression `ImpBoolExpr` is compositionally correct with respect to its constructors.
  Sketch: example (b1 b2 : ImpBoolExpr) (env : ImpEnv) : (ImpBoolExpr.and b1 b2).eval env = (b1.eval env && b2.eval env) := by slim_check
  SampleableExt: yes
  Risk: The boolean logic interpreter (`ImpBoolExpr.eval`) could be flawed, causing incorrect control flow in `if` and `while` statements.

- [P3] P0 EQUIVALENCE: The core compilation correctness theorem holds. Evaluating a statement in the `Imp` language yields the same final state as compiling it to the `Core` language and evaluating it there.
  Sketch: forall (s : ImpStmt) (env : ImpEnv), s.eval env = (s.compile).eval env := by slim_check
  SampleableExt: yes
  Risk: The entire purpose of the compiler is defeated if it does not preserve the semantics of the original program. This is the primary soundness check.

- [P4] P1 INVARIANT: The `skip` statement is a true no-op; evaluating it does not alter the environment.
  Sketch: forall (env : ImpEnv), (ImpStmt.skip).eval env = env := by slim_check
  SampleableExt: yes
  Risk: If `skip` is not a no-op, it could introduce subtle state corruption, especially in code generated from more complex frontends.

- [P5] P1 EQUIVALENCE: Statement sequencing (`seq`) is associative. The order of evaluation for a chain of three statements doesn't depend on the grouping.
  Sketch: forall (s1 s2 s3 : ImpStmt) (env : ImpEnv), (ImpStmt.seq (ImpStmt.seq s1 s2) s3).eval env = (ImpStmt.seq s1 (ImpStmt.seq s2 s3)).eval env := by slim_check
  SampleableExt: yes
  Risk: Incorrect implementation of `seq` would break the fundamental composition of programs, leading to unpredictable execution order.

- [P6] P1 INVARIANT: The `eval` function for statements is deterministic. Given the same statement and initial environment, it always produces the same final environment.
  Sketch: forall (s : ImpStmt) (env : ImpEnv), s.eval env = s.eval env := by slim_check
  SampleableExt: yes
  Risk: Non-determinism in the interpreter would make program behavior unpredictable and proofs of correctness impossible.

INTEGRATION:
- [T1] BASIC: Simple sequential assignment program.
  Setup: Let s := `ImpStmt.seq (ImpStmt.assign "x" (ImpExpr.const 10)) (ImpStmt.assign "y" (ImpExpr.add (ImpExpr.var "x") (ImpExpr.const 5)))`. Evaluate `s.eval` on an empty `ImpEnv`.
  Check: The resulting environment maps "x" to 10 and "y" to 15.

- [T2] BASIC: `if` statement with a true condition.
  Setup: Let s := `ImpStmt.if (ImpBoolExpr.le (ImpExpr.const 5) (ImpExpr.const 10)) (ImpStmt.assign "x" (ImpExpr.const 1)) (ImpStmt.assign "x" (ImpExpr.const 0))`. Evaluate `s.eval` on an empty `ImpEnv`.
  Check: The resulting environment maps "x" to 1.

- [T3] BASIC: `if` statement with a false condition.
  Setup: Let s := `ImpStmt.if (ImpBoolExpr.le (ImpExpr.const 10) (ImpExpr.const 5)) (ImpStmt.assign "x" (ImpExpr.const 1)) (ImpStmt.assign "x" (ImpExpr.const 0))`. Evaluate `s.eval` on an empty `ImpEnv`.
  Check: The resulting environment maps "x" to 0.

- [T4] BASIC: `while` loop that executes multiple times (factorial-like).
  Setup: Let s be a program that calculates `3 + 2 + 1`: `x := 3; total := 0; while not (x = 0) do (total := total + x; x := x - 1)`. Evaluate `s.eval` on an empty `ImpEnv`.
  Check: The resulting environment maps "x" to 0 and "total" to 6.

- [T5] EDGE_CASE: `while` loop whose condition is initially false.
  Setup: Let s := `ImpStmt.while (ImpBoolExpr.false) (ImpStmt.assign "x" (ImpExpr.const 1))`. Evaluate `s.eval` on an empty `ImpEnv`.
  Check: The resulting environment is empty (the loop body never runs).

- [T6] EDGE_CASE: Reading an uninitialized variable.
  Setup: Let s := `ImpStmt.assign "x" (ImpExpr.add (ImpExpr.var "y") (ImpExpr.const 5))`. Evaluate `s.eval` on an empty `ImpEnv`.
  Check: The resulting environment maps "x" to 5, confirming that reading a non-existent variable defaults to 0.

- [T7] EDGE_CASE: Overwriting an existing variable.
  Setup: Let s := `ImpStmt.seq (ImpStmt.assign "x" (ImpExpr.const 10)) (ImpStmt.assign "x" (ImpExpr.add (ImpExpr.var "x") (ImpExpr.const 1)))`. Evaluate `s.eval` on an empty `ImpEnv`.
  Check: The resulting environment maps "x" to 11.

- [T8] EDGE_CASE: Program consisting only of `skip`.
  Setup: Let s := `ImpStmt.skip`. Evaluate `s.eval` on an `ImpEnv` mapping "x" to 42.
  Check: The resulting environment is unchanged, still mapping "x" to 42.

- [T9] STRESS: Deeply nested `seq` and `if` statements.
  Setup: Construct a statement with 100 nested `if true then ... else skip` blocks, with a final assignment `x := 1` in the innermost block.
  Check: Evaluation completes without a stack overflow and the final environment maps "x" to 1.

- [T10] STRESS: `while` loop with many iterations.
  Setup: A countdown loop similar to T4, but starting from `x := 500`.
  Check: Evaluation completes in a reasonable time and the final "total" is the sum of integers from 1 to 500 (125250).

- [T11] REGRESSION: Correctness theorem holds for a concrete `while` loop.
  Setup: Let `s` be the `while` loop program from T4. Let `s_compiled := s.compile`. Let `env` be an empty `ImpEnv`.
  Check: The value of `s.eval env` is definitionally equal to the value of `s_compiled.eval env`. This can be checked with `#eval (s.eval env == s_compiled.eval env)`. The expected result is `true`.

---

### N3.1 — Bridge ExpandedSigma

- **Tipo**: CRITICO
- **Archivos fuente**: `TrustLean/Bridge/Types.lean`, `TrustLean/Bridge/ExprTranslation.lean`, `TrustLean/Bridge/ScalarTranslation.lean`, `TrustLean/Bridge/MemoryTranslation.lean`, `TrustLean/Bridge/Compile.lean`, `TrustLean/Bridge/Semantics.lean`, `TrustLean/Bridge/Correctness.lean`
- **Target properties**: `Tests/Properties/BridgeExpandedSigma.lean`
- **Target integration**: `Tests/Integration/BridgeExpandedSigma.lean`
- **Properties**: 5 (3 P0)
- **Integration tests**: 7

PROPERTIES:
- [P1] P0 SOUNDNESS: The evaluation of a translated `ScalarExpr` in a "bridged" environment should be equal to the evaluation of the original `ScalarExpr` in the original environment.
  Sketch: example (e : ScalarExpr) (env : SigmaEnv) : evalLLExpr (scalarExprToLLExpr e) (fullBridge env) = evalScalarExpr e env := by slim_check
  SampleableExt: yes
  Risk: Incorrect code generation for arithmetic operations. Calculations in the compiled code would produce wrong results, violating the compiler's correctness.

- [P2] P0 SOUNDNESS: The evaluation of a translated `IdxExpr` in a "bridged" environment should be equal to the evaluation of the original `IdxExpr` in the original environment.
  Sketch: example (e : IdxExpr) (env : SigmaEnv) : evalLLExpr (idxExprToLLExpr e) (fullBridge env) = evalIdxExpr e env := by slim_check
  SampleableExt: yes
  Risk: Incorrect memory addressing. The program would read from or write to the wrong memory locations, leading to data corruption, segmentation faults, or other severe runtime errors.

- [P3] P0 INVARIANT: The name-mangling scheme for variables must be injective and produce disjoint name sets for different variable types (scalar, loop, memory).
  Sketch: example (v1 v2 : ScalarVar) (h : v1 ≠ v2) : scalarVarToVarName v1 ≠ scalarVarToVarName v2 := by slim_check
    example (sv : ScalarVar) (lv : LoopVar) : scalarVarToVarName sv ≠ loopVarToVarName lv := by slim_check
    example (sv : ScalarVar) : scalarVarToVarName sv ≠ memArrayName := by slim_check
  SampleableExt: yes
  Risk: Catastrophic variable aliasing. If distinct source variables map to the same target name, assignments to one will silently corrupt the other, leading to unpredictable and incorrect program behavior.

- [P4] P1 PRESERVATION: The `scalarExprToLLExpr` translation must be a homomorphism for arithmetic operations, preserving the algebraic structure of expressions.
  Sketch: example (a b : ScalarExpr) :
    (scalarExprToLLExpr (.add a b) = .binop .add (scalarExprToLLExpr a) (scalarExprToLLExpr b)) ∧
    (scalarExprToLLExpr (.sub a b) = .binop .sub (scalarExprToLLExpr a) (scalarExprToLLExpr b)) ∧
    (scalarExprToLLExpr (.mul a b) = .binop .mul (scalarExprToToLLExpr a) (scalarExprToLLExpr b)) := by slim_check
  SampleableExt: yes
  Risk: The translation logic could be subtly incorrect, for example, mis-mapping an operator or failing to handle precedence correctly in a more complex target language, leading to incorrect calculations.

- [P5] P1 PRESERVATION: Updating a value in the `SigmaEnv` (e.g., a scalar variable) must only affect the corresponding value in the `fullBridge`d environment, leaving all other unrelated bridged values unchanged (frame property).
  Sketch: example (env : SigmaEnv) (sv : ScalarVar) (val : Value) (lv : LoopVar) : (fullBridge (env.updateScalar sv val)) (loopVarToVarName lv) = (fullBridge env) (loopVarToVarName lv) := by slim_check
  SampleableExt: yes
  Risk: State corruption. A bug in the state bridging logic could cause an update to one variable to "bleed over" and incorrectly change the value of another, leading to a corrupted program state and invalid results.

INTEGRATION:
- [T1] BASIC: Translate and evaluate a simple `ScalarExpr` with a variable and a literal.
  Setup:
    def sv_in := ScalarVar.mk .Input 0
    def expr := ScalarExpr.add (.var sv_in) (.lit (Value.i 42))
    def env : SigmaEnv := { scalar_vars := fun v => if v = sv_in then some (Value.i 10) else none, loop_vars := default, mem := default }
  Check: `evalLLExpr (scalarExprToLLExpr expr) (fullBridge env)` should evaluate to `some (Value.i 52)`.

- [T2] BASIC: Translate and evaluate a nested `ScalarExpr` involving multiple variables and operations.
  Setup:
    def sv_in0 := ScalarVar.mk .Input 0
    def sv_in1 := ScalarVar.mk .Input 1
    def expr := ScalarExpr.mul (.sub (.var sv_in0) (.lit (Value.i 5))) (.add (.var sv_in1) (.lit (Value.i 2)))
    def env : SigmaEnv := { scalar_vars := fun v => if v = sv_in0 then some (Value.i 10) else if v = sv_in1 then some (Value.i 3) else none, loop_vars := default, mem := default }
  Check: `evalLLExpr (scalarExprToLLExpr expr) (fullBridge env)` should evaluate to `some (Value.i 25)`.

- [T3] BASIC: Translate and evaluate a simple `IdxExpr` involving a loop variable.
  Setup:
    def lv0 : LoopVar := ⟨0, 10⟩
    def expr := IdxExpr.add (.var lv0) (.lit 5)
    def env : SigmaEnv := { scalar_vars := default, loop_vars := fun v => if v = lv0 then some 3 else none, mem := default }
  Check: `evalLLExpr (idxExprToLLExpr expr) (fullBridge env)` should evaluate to `some (Value.i 8)`.

- [T4] EDGE_CASE: Translate and evaluate a `ScalarExpr` containing only literals (a constant expression).
  Setup:
    def expr := ScalarExpr.add (.lit (Value.i 100)) (.lit (Value.i 200))
    def env : SigmaEnv := default
  Check: `evalLLExpr (scalarExprToLLExpr expr) (fullBridge env)` should evaluate to `some (Value.i 300)`.

- [T5] EDGE_CASE: Check `fullBridge` on a completely empty `SigmaEnv`.
  Setup:
    def env : SigmaEnv := default
    def bridged_env := fullBridge env
  Check: `bridged_env "any_var"` should be `none`, and `bridged_env memArrayName` should be `some (.arr #[])`.

- [T6] BASIC: Verify that `fullBridge` correctly maps all components of a populated `SigmaEnv`.
  Setup:
    def sv_in0 := ScalarVar.mk .Input 0
    def lv0 : LoopVar := ⟨0, 10⟩
    def mem_init : Array Value := #[.i 1, .i 2, .i 3]
    def env : SigmaEnv := { scalar_vars := fun v => if v = sv_in0 then some (.i 100) else none, loop_vars := fun v => if v = lv0 then some 5 else none, mem := mem_init }
    def bridged_env := fullBridge env
  Check:
    `bridged_env (scalarVarToVarName sv_in0)` should be `some (.i 100)`.
    `bridged_env (loopVarToVarName lv0)` should be `some (.i 5)`.
    `bridged_env memArrayName` should be `some (.arr mem_init)`.

- [T7] BASIC: Verify the generated names for different scalar variable kinds and loop variables.
  Setup:
    def v_in := ScalarVar.mk .Input 5
    def v_out := ScalarVar.mk .Output 5
    def v_tmp := ScalarVar.mk .Temp 5
    def lv := LoopVar.mk 3 100
  Check:
    `scalarVarToVarName v_in` should be `"in5"`.
    `scalarVarToVarName v_out` should be `"out5"`.
    `scalarVarToVarName v_tmp` should be `"tmp5"`.
    `loopVarToVarName lv` should be `"loop3"`.
    All generated names should be distinct from each other and from `memArrayName`.

---

### N3.2 — Pipeline + RustBackend

- **Tipo**: HOJA
- **Archivos fuente**: `TrustLean/Pipeline.lean`, `TrustLean/Backend/RustBackend.lean`
- **Target properties**: `Tests/Properties/Pipeline+RustBackend.lean`
- **Target integration**: `Tests/Integration/Pipeline+RustBackend.lean`
- **Properties**: 6 (2 P0)
- **Integration tests**: 10

PROPERTIES:
- [P1] P0 INVARIANT: The `stmtToRust` function produces a string with balanced curly braces `{}`.
  Sketch: example (s : Stmt) : balancedBraces (stmtToRust s) := by slim_check
  SampleableExt: yes
  Risk: Generated code will not compile due to syntax errors.

- [P2] P0 EQUIVALENCE: The `stmtToRust` function is deterministic.
  Sketch: example (s : Stmt) (cfg : RustConfig) : stmtToRust cfg s = stmtToRust cfg s := by slim_check
  SampleableExt: yes
  Risk: Non-deterministic code generation can lead to unreproducible builds and hard-to-diagnose bugs.

- [P3] P1 PRESERVATION: The translation of a `Stmt` constructor preserves the corresponding control flow keyword.
  Sketch: example (cond : Expr) (body : Stmt) (cfg : RustConfig) : (stmtToRust cfg (.while cond body)).contains "while" := by slim_check
  SampleableExt: yes
  Risk: Gross mis-translation of the program's control flow structure (e.g., a `while` loop is emitted as an `if` statement).

- [P4] P1 EQUIVALENCE: The translation of `if true then s1 else s2` should be syntactically equivalent to the translation of just `s1`.
  Sketch: example (s1 s2 : Stmt) (cfg : RustConfig) : stmtToRust cfg (.if (.lit (.bool true)) s1 s2) = stmtToRust cfg s1 := by slim_check
  SampleableExt: yes
  Risk: The backend fails to perform basic constant folding, leading to inefficient and unreadable code with dead branches.

- [P5] P1 IDEMPOTENCY: The identifier sanitization function is idempotent.
  Sketch: example (s : String) : sanitizeIdentifier (sanitizeIdentifier s) = sanitizeIdentifier s := by slim_check
  SampleableExt: no
  Risk: Multiple applications of the sanitizer (e.g., in different pipeline stages) could lead to incorrect or mangled identifiers like `__var_1_`.

- [P6] P2 IDEMPOTENCY: The `Pipeline.lower` transformation is idempotent.
  Sketch: example (c : CodeGenerable) : Pipeline.lower (Pipeline.lower c) = Pipeline.lower c := by slim_check
  SampleableExt: yes
  Risk: If the lowering pass is accidentally run multiple times, it could produce incorrect or unexpectedly transformed code.

INTEGRATION:
- [T1] BASIC: The pipeline can generate a complete, minimal, and compilable Rust program.
  Setup: A `CodeGenerable` instance for a function `main` with an empty body (`.skip`).
  Check: `Pipeline.emit` produces a string that is a valid Rust program (e.g., contains `fn main() {}`) and can be compiled with `rustc` without warnings or errors.

- [T2] EDGE_CASE: An `if` statement with an empty `else` branch is handled correctly.
  Setup: `Stmt.if cond then_branch .skip`
  Check: The generated code contains an `if cond { ... }` block with no `else` part.

- [T3] BASIC: All binary operators are translated correctly.
  Setup: Create a sequence of `assign` statements, one for each `BinOp` constructor (add, sub, mul, etc.), assigning the result to a variable.
  Check: The generated Rust code contains the correct corresponding binary operator for each assignment (e.g., `+`, `-`, `*`, `==`, `&&`).

- [T4] BASIC: A sequence of statements is translated into a correctly ordered block of Rust statements.
  Setup: `Stmt.seq (.assign "x" 1) (.seq (.assign "y" 2) (.assign "x" 3))`
  Check: The generated code contains `let mut x = 1; let mut y = 2; x = 3;` in that specific order.

- [T5] EDGE_CASE: An identifier that is a Rust keyword is correctly sanitized.
  Setup: A `Stmt` that assigns to a variable named `match`, e.g., `.assign "match" (.lit (.int 42))`.
  Check: The generated code uses a sanitized identifier, such as `r#match` or `match_`, which is a valid Rust variable name (e.g., `let mut r#match = 42;`).

- [T6] BASIC: The `RustConfig` options correctly influence the generated code.
  Setup: Define a `CodeGenerable` function that takes an integer argument. Call `Pipeline.emit` twice: once with default `RustConfig`, and once with a `RustConfig` where `intType := "i64"`.
  Check: The first output uses the default integer type (e.g., `i32`) in the function signature, while the second output uses `i64`.

- [T7] STRESS: Deeply nested control flow structures are generated with balanced braces.
  Setup: A `Stmt` with 5+ levels of nested `if` and `while` statements.
  Check: The output is a valid Rust snippet with correctly matched curly braces `{}`. This complements property P1 with a complex, concrete example.

- [T8] BASIC: Memory `store` and `load` operations are translated into correct Rust syntax.
  Setup: A `Stmt` that stores the result of a `load` operation, e.g., `.store base1 offset1 (.load base2 offset2)`. Assume the memory model corresponds to array/slice access.
  Check: The generated code is syntactically valid Rust, like `base1[offset1] = base2[offset2];`. Pay special attention to operator precedence if offsets are complex expressions.

- [T9] REGRESSION: A `while` loop with a `false` literal condition is correctly handled.
  Setup: `Stmt.while (.lit (.bool false)) body`.
  Check: The generated code should ideally be an empty block or be completely omitted, demonstrating dead code elimination. A lesser requirement is that it compiles to a loop that never executes.

- [T10] BASIC: A function with multiple arguments and a return value is generated correctly.
  Setup: A `CodeGenerable` for a function `add` that takes two integers `a` and `b`, and has a body `.return (.binOp .add (.var "a") (.var "b"))`.
  Check: The generated Rust code is a complete function `fn add(a: i32, b: i32) -> i32 { return a + b; }` (or with types from `RustConfig`).

---

### N9.1 — Sanitización y Helpers

- **Tipo**: FUNDACIONAL
- **Archivos fuente**: `TrustLean/Backend/Common.lean`
- **Target properties**: `Tests/Properties/SanitizaciónyHelpers.lean`
- **Target integration**: `Tests/Integration/SanitizaciónyHelpers.lean`
- **Properties**: 4 (1 P0)
- **Integration tests**: 12

PROPERTIES:
- [P1] P0 IDEMPOTENCY: Applying `sanitizeIdentifier` to a string that has already been sanitized should not change the string. This ensures the sanitization process is stable and predictable.
  Sketch: property (s : String) : sanitizeIdentifier (sanitizeIdentifier s) = sanitizeIdentifier s := by slim_check
  SampleableExt: no
  Risk: Unpredictable identifier generation in code generation pipelines. If the function is not idempotent, applying a transformation pass multiple times could yield different results, breaking determinism.

- [P2] P1 PRESERVATION: If a string is already a valid C identifier and not a reserved keyword, `sanitizeIdentifier` should return it unchanged. This prevents unnecessary mangling of valid names.
  Sketch: property (s : String) (h_valid : isValidCIdent s) (h_not_kw : s ∉ cReservedIdentifiers) : sanitizeIdentifier s = s := by slim_check
  SampleableExt: yes
  Risk: The sanitizer might be too aggressive, unnecessarily changing valid user-provided names, which would lead to less readable and harder-to-debug generated C code.

- [P3] P1 INVARIANT: If an input string is a C keyword, or is not a valid C identifier (e.g., starts with a digit, is empty), the sanitized output must start with the designated `tl_` prefix to avoid name collisions.
  Sketch: property (s : String) (h : s ∈ cReservedIdentifiers ∨ ¬ isValidCIdent s) : (sanitizeIdentifier s).startsWith "tl_" := by slim_check
  SampleableExt: yes
  Risk: Name clashes in the generated C code. If the prefixing logic fails, a sanitized keyword (e.g., "for") could become another valid identifier, leading to compilation errors or subtle runtime bugs.

- [P4] P2 SOUNDNESS: The `joinCode` function should correctly join a list of strings with newline characters. The resulting string's length should be the sum of the lengths of the input strings plus the number of separators.
  Sketch: property (ls : List String) (h_nonempty : ls.length > 0) : (joinCode ls).length = (ls.map String.length).sum + ls.length - 1 := by slim_check
  SampleableExt: no
  Risk: Incorrectly formatted C code blocks, leading to compilation errors. This is a low-risk, simple helper, but its correctness is foundational.

INTEGRATION:
- [T1] BASIC: `sanitizeIdentifier` correctly handles a simple, valid identifier, leaving it unchanged.
  Setup: let s := "my_variable_name"
  Check: `sanitizeIdentifier s` equals `"my_variable_name"`.

- [T2] EDGE_CASE: `sanitizeIdentifier` correctly handles a C99 keyword by prefixing it.
  Setup: let s := "while"
  Check: `sanitizeIdentifier s` equals `"tl_while"`.

- [T3] EDGE_CASE: `sanitizeIdentifier` correctly handles an identifier that starts with a number by prefixing it.
  Setup: let s := "2_turtles"
  Check: `sanitizeIdentifier s` equals `"tl_2_turtles"`.

- [T4] EDGE_CASE: `sanitizeIdentifier` correctly handles an identifier containing invalid characters by stripping them.
  Setup: let s := "a-b(c)d!e"
  Check: `sanitizeIdentifier s` equals `"abcde"`.

- [T5] EDGE_CASE: `sanitizeIdentifier` handles an identifier that becomes a keyword *after* stripping invalid characters.
  Setup: let s := "v*o*i*d"
  Check: `sanitizeIdentifier s` equals `"tl_void"`.

- [T6] EDGE_CASE: `sanitizeIdentifier` handles an empty string input.
  Setup: let s := ""
  Check: `sanitizeIdentifier s` equals `"tl_v"`.

- [T7] EDGE_CASE: `sanitizeIdentifier` handles a string that becomes empty after stripping invalid characters.
  Setup: let s := "!@#$%^"
  Check: `sanitizeIdentifier s` equals `"tl_v"`.

- [T8] BASIC: `indentStr` correctly adds a specified number of spaces to a string.
  Setup: let s := "code_line"
  Check: `indentStr 4 s` equals `"    code_line"`.

- [T9] BASIC: `joinCode` correctly joins a list of strings with newlines.
  Setup: let lines := ["int main() {", "  return 0;", "}"]
  Check: `joinCode lines` equals `"int main() {\n  return 0;\n}"`.

- [T10] EDGE_CASE: `joinCode` handles an empty list of strings.
  Setup: let lines : List String := []
  Check: `joinCode lines` equals `""`.

- [T11] EDGE_CASE: `joinCode` handles a list with a single string.
  Setup: let lines := ["one_line"]
  Check: `joinCode lines` equals `"one_line"`.

- [T12] BASIC: `formatArrayAccess` correctly formats a base and index string into C-style array access.
  Setup: let base := "my_array", let idx := "i + 1"
  Check: `formatArrayAccess base idx` equals `"my_array[i + 1]"`.

---

### N9.2 — CBackend Refactor

- **Tipo**: CRITICO
- **Archivos fuente**: `TrustLean/Backend/CBackend.lean`
- **Target properties**: `Tests/Properties/CBackendRefactor.lean`
- **Target integration**: `Tests/Integration/CBackendRefactor.lean`
- **Properties**: 6 (3 P0)
- **Integration tests**: 12

PROPERTIES:
- [P1] P0 INVARIANT: Sanitized identifiers must not be C keywords.
  Sketch: example (s : String) : varNameToC s ∉ cKeywords := by slim_check
  SampleableExt: no
  Risk: Generated C code fails to compile due to syntax errors (e.g., `int int = 5;`).

- [P2] P0 INVARIANT: Sanitized identifiers must be valid C identifiers.
  Sketch: example (s : String) : isValidCIdentifier (varNameToC s) := by slim_check
  SampleableExt: no
  Risk: Generated C code fails to compile due to invalid identifiers (e.g., `int 1_var = 5;`).

- [P3] P1 IDEMPOTENCY: Identifier sanitization is idempotent.
  Sketch: example (s : String) : varNameToC (varNameToC s) = varNameToC s := by slim_check
  SampleableExt: no
  Risk: Unnecessary complexity or unexpected transformations if the sanitization function is not stable.

- [P4] P0 INVARIANT: Generated statement blocks have balanced braces.
  Sketch: example (s : Stmt) : (stmtToC s).count '{' = (stmtToC s).count '}' := by slim_check
  SampleableExt: yes
  Risk: Malformed C code with compilation errors due to unbalanced scopes.

- [P5] P1 SOUNDNESS: Integer constants are correctly represented as strings.
  Sketch: example (n : Int) : (exprToC (Expr.Const (Const.Int n))).contains n.toString := by slim_check
  SampleableExt: no
  Risk: Incorrect program logic due to wrong constant values being emitted.

- [P6] P2 INVARIANT: Generating a function with a non-empty body produces a non-empty string.
  Sketch: example (f : FunctionDefinition) (h : f.body.length > 0) : (generateCFunction f CConfig.default).length > 0 := by slim_check
  SampleableExt: yes
  Risk: The backend could silently fail to generate code for some functions, leading to missing implementations.

INTEGRATION:
- [T1] BASIC: Simple assignment statement generation.
  Setup: Construct an `Stmt.Assign` where the right-hand side is a binary operation, e.g., `x = y + 1`.
  Check: The output of `stmtToC` is the string `"x = y + 1;"` (assuming variable names don't need sanitization).

- [T2] BASIC: If-Then-Else statement generation.
  Setup: Construct an `Stmt.If` with a condition, a `then` block, and an `else` block.
  Check: The output string has the correct C structure: `if (condition) { ... } else { ... }`. Verify correct brace placement and statement nesting.

- [T3] BASIC: For loop generation.
  Setup: Construct an `Stmt.For` with a loop variable, start/end expressions, and a body.
  Check: The output string is a valid C for loop, e.g., `for (int i = 0; i < 10; ++i) { ... }`.

- [T4] BASIC: All `BinOp` and `UnaryOp` variants are correctly translated.
  Setup: Create a list of `Expr`s, one for each `BinOp` (Add, Sub, Mul, Div, Eq, Lt, etc.) and `UnaryOp` (Not, Neg).
  Check: The output of `exprToC` for each expression contains the expected C operator (`+`, `-`, `*`, `==`, `!`, etc.).

- [T5] BASIC: `Store` and `Load` expressions are correctly parenthesized.
  Setup: Construct an `Expr.Load` and an `Stmt.Store` with a variable as the base pointer and a constant offset.
  Check: The generated C code correctly parenthesizes the base pointer as specified in the rubric, e.g., `*((base) + offset)`.

- [T6] EDGE_CASE: Variable names that are C keywords are sanitized.
  Setup: Construct a statement using a variable named `int`, `for`, or another C keyword.
  Check: The generated C code uses a sanitized name (e.g., `_int`, `_for`) for the variable.

- [T7] EDGE_CASE: Variable names with invalid C characters are sanitized.
  Setup: Construct a statement using a variable name with invalid characters, like `a-b` or `c$d`.
  Check: The generated C code uses a sanitized name where invalid characters are replaced (e.g., `a_b`, `c_d`).

- [T8] EDGE_CASE: Control flow statements with empty bodies.
  Setup: Construct an `Stmt.If` and an `Stmt.For` where the body is an empty block.
  Check: The generated C code is syntactically valid, preferably `if (...) {}` and `for (...) {}`.

- [T9] EDGE_CASE: Function with no arguments and an empty body.
  Setup: Construct a `FunctionDefinition` with an empty argument list and an empty body.
  Check: `generateCFunction` produces a valid, empty C function, e.g., `void my_func(void) {}`.

- [T10] BASIC: Full function and header generation.
  Setup: Construct a complete `FunctionDefinition` with arguments, a return type, and a non-trivial body.
  Check: `generateCFunction` produces a valid C function string. `generateCHeader` produces a valid C header string that includes `stdint.h` and `stdbool.h`.

- [T11] STRESS: Compilation of generated code with `gcc -Wall -Werror`.
  Setup: Generate a C source and header file for a function containing a representative mix of all statement and expression types (nested loops, conditionals, memory access, various operators).
  Check: The test writes the generated strings to temporary files and executes `gcc -Wall -Werror -c` on the source file. The compiler must exit with code 0.

- [T12] REGRESSION: Operator precedence is handled correctly.
  Setup: Construct an expression involving operators with different precedence levels, e.g., `a + b * c`.
  Check: The output of `exprToC` is `a + b * c`, without incorrect parentheses like `(a + b) * c`. The backend should rely on C's native precedence rules.

---

### N9.3 — Propiedades Formales CBackend

- **Tipo**: PARALELO
- **Archivos fuente**: `TrustLean/Backend/CBackendProperties.lean`
- **Target properties**: `Tests/Properties/PropiedadesFormalesCBackend.lean`
- **Target integration**: `Tests/Integration/PropiedadesFormalesCBackend.lean`
- **Properties**: 6 (3 P0)
- **Integration tests**: 8

An appropriate test plan for this PARALELO node will focus on the correctness and consistency of the C code generation. We will verify key invariants using property-based tests and check the output for all language constructs using specific integration tests.

### A. Property Tests (SlimCheck)

These tests verify fundamental properties that must hold true for all possible inputs, ensuring the C backend is robust and correct by construction.

```
PROPERTIES:
- [P1] P0 INVARIANT: The C code generated for any statement has balanced curly braces.
  Sketch: theorem stmtToC_braces_balanced (s : Stmt) : countChar '{' (stmtToC s) = countChar '}' (stmtToC s) := by slim_check
  SampleableExt: yes
  Risk: Syntactically invalid C code leading to compilation failures and incorrect program logic due to wrong scoping.

- [P2] P0 INVARIANT: The `sanitizeIdentifier` function produces a valid C identifier.
  Sketch: theorem sanitizeIdentifier_produces_valid_c_identifier (s : String) : isValidCIdentifier (sanitizeIdentifier s) := by slim_check
  SampleableExt: no
  Risk: Generated C code is syntactically invalid and will not compile.

- [P3] P0 INVARIANT: The `sanitizeIdentifier` function never produces a C keyword.
  Sketch: theorem sanitizeIdentifier_avoids_keywords (s : String) : (sanitizeIdentifier s) ∉ cKeywords := by slim_check
  SampleableExt: no
  Risk: Generated C code is syntactically invalid due to keyword misuse, causing compilation errors.

- [P4] P1 IDEMPOTENCY: The `sanitizeIdentifier` function is idempotent.
  Sketch: theorem sanitizeIdentifier_idempotent (s : String) : sanitizeIdentifier (sanitizeIdentifier s) = sanitizeIdentifier s := by slim_check
  SampleableExt: no
  Risk: Inconsistent variable naming if a name is sanitized multiple times, potentially breaking code generation logic that relies on stable names.

- [P5] P1 SOUNDNESS: The top-level program-to-C translation (`progToC`) is a pure function.
  Sketch: theorem progToC_deterministic (p : Prog) : progToC p = progToC p := by slim_check
  SampleableExt: yes
  Risk: Non-reproducible builds or heisenbugs if the C backend has hidden state or non-determinism.

- [P6] P2 INVARIANT: The C code generated for any expression has balanced parentheses.
  Sketch: theorem exprToC_parens_balanced (e : Expr) : countChar '(' (exprToC e) = countChar ')' (exprToC e) := by slim_check
  SampleableExt: yes
  Risk: Malformed C expressions leading to compilation errors or incorrect order of operations.
```

### B. Integration Tests (#eval)

These tests check the concrete output of the C backend for specific, representative language constructs, covering happy paths, edge cases, and all 12 `Stmt` types as required by the rubric.

```
INTEGRATION:
- [T1] BASIC: `sanitizeIdentifier` correctly handles valid names, C keywords, and invalid characters.
  Setup: let s1 := "my_var", let s2 := "while", let s3 := "var-name"
  Check: `#eval sanitizeIdentifier s1` equals `"my_var"`. `#eval sanitizeIdentifier s2` equals `"while_"`. `#eval sanitizeIdentifier s3` equals `"var_name"`.

- [T2] BASIC: `generateCHeader` includes required standard C headers.
  Setup: let p : Prog := { funcs := [], main := .skip }
  Check: `#eval generateCHeader p` contains the substrings `"stdint.h"` and `"stdbool.h"`.

- [T3] BASIC: All 12 `Stmt` constructors produce valid C code snippets.
  Setup: Create a separate `Stmt` instance for each constructor: `decl`, `assign`, `ite`, `while`, `for`, `seq`, `skip`, `return`, `assert`, `store`, `load` (as part of an expr), and `expr` (for function calls).
  Check: `#eval stmtToC` for each instance produces the expected C code string. For example, `Stmt.decl` produces `"int64_t x;"`, `Stmt.skip` produces `";"`, etc.

- [T4] EDGE_CASE: `Stmt.ite` without an `else` block (using `Stmt.skip`) generates a simple `if`.
  Setup: let cond := Expr.lit (.bool true), let then_b := Stmt.assign "x" (Expr.lit (.int 1)), let ite_stmt := Stmt.ite cond then_b .skip
  Check: `#eval stmtToC ite_stmt` produces an `if (...) { ... }` block without a corresponding `else` block.

- [T5] EDGE_CASE: `store` and `load` expressions correctly parenthesize a complex base address.
  Setup: let base := Expr.op .add (Expr.var "arr") (Expr.var "offset"), let idx := Expr.lit (.int 0), let store_stmt := Stmt.store base idx (Expr.lit (.int 100))
  Check: `#eval stmtToC store_stmt` equals `"*((arr + offset) + 0) = 100;"`. The parentheses around `arr + offset` are critical.

- [T6] EDGE_CASE: An empty program or a program with an empty `main` function compiles to a valid minimal C program.
  Setup: let p : Prog := { funcs := [], main := .skip }
  Check: `#eval progToC p` produces a string containing `int main(void) { ... return 0; }` which is syntactically valid.

- [T7] STRESS: Deeply nested control flow statements maintain correct brace structure and indentation.
  Setup: Construct a `Stmt` with 5 levels of nesting, e.g., `for { while { if { ... } } }`.
  Check: The output of `stmtToC` is syntactically correct C. A visual inspection should confirm correct structure. The property test P1 provides the formal guarantee of balanced braces.

- [T8] BASIC: All binary and unary expression operators are translated to their C equivalents.
  Setup: Create a list of `Expr`s, one for each operator (e.g., `add`, `sub`, `mul`, `div`, `eq`, `lt`, `and`, `or`, `not`).
  Check: Map `exprToC` over the list and verify that each output string matches the expected C operator (e.g., `+`, `-`, `*`, `/`, `==`, `<`, `&&`, `||`, `!`).
```

---

### N9.4 — Integration Tests + Regression

- **Tipo**: PARALELO
- **Archivos fuente**: `TrustLean/Tests/CBackendIntegration.lean`
- **Target properties**: `Tests/Properties/IntegrationTests+Regression.lean`
- **Target integration**: `Tests/Integration/IntegrationTests+Regression.lean`
- **Properties**: 5 (3 P0)
- **Integration tests**: 16

PROPERTIES:
- [P1] P0 IDEMPOTENCY: The `sanitizeIdentifier` function is idempotent, meaning applying it twice has the same effect as applying it once.
  Sketch: example (s : String) : sanitizeIdentifier (sanitizeIdentifier s) = sanitizeIdentifier s := by slim_check
  SampleableExt: yes
  Risk: Unnecessary complexity or mangling of already-sanitized names in generated code, leading to compilation errors or incorrect variable references.

- [P2] P0 INVARIANT: The output of `sanitizeIdentifier` is always a valid C identifier.
  Sketch: example (s : String) : isValidCIdentifier (sanitizeIdentifier s) := by slim_check
  SampleableExt: yes
  Risk: The C backend generates syntactically invalid code that fails to compile.

- [P3] P0 INVARIANT: The output of `sanitizeIdentifier` is never a C keyword.
  Sketch: example (s : String) : (sanitizeIdentifier s) ∉ cKeywords := by slim_check
  SampleableExt: yes
  Risk: Generated code uses a C keyword (e.g., "if", "while") as a variable name, causing a compilation error.

- [P4] P1 SOUNDNESS: The `stmtToC` function produces C code with balanced curly braces.
  Sketch: example (stmt : Stmt) : countChar '{' (stmtToC stmt) = countChar '}' (stmtToC stmt) := by slim_check
  SampleableExt: yes
  Risk: Malformed C code with syntax errors related to scope blocks, leading to compilation failure.

- [P5] P2 EQUIVALENCE: The `stmtToC` function is deterministic for any given statement.
  Sketch: example (stmt : Stmt) : stmtToC stmt = stmtToC stmt := by slim_check
  SampleableExt: yes
  Risk: Non-determinism in code generation could make debugging and builds unreliable, though this is unlikely in a pure functional setting.

INTEGRATION:
- [T1] BASIC: Simple arithmetic and variable assignment.
  Setup: A program that declares two integer variables `x` and `y`, assigns them values, computes their sum into a third variable `z`, and prints `z`.
  Check: The compiled C program prints the correct sum to standard output.

- [T2] BASIC: Conditional `if-then-else` logic.
  Setup: A program that declares a boolean variable, uses an `if` statement to check its value, and prints "true branch" or "false branch" accordingly. Run twice, once for each branch.
  Check: The compiled program prints the correct string for each case.

- [T3] BASIC: `while` loop functionality.
  Setup: A program that initializes a counter to 0 and uses a `while` loop to increment it to 5, printing the counter's value at each iteration.
  Check: The compiled program prints "0\n1\n2\n3\n4\n".

- [T4] BASIC: Array declaration, store, and load.
  Setup: A program that declares an integer array of size 10, stores the value 42 at index 3, then loads the value from index 3 and prints it.
  Check: The compiled program prints "42".

- [T5] EDGE_CASE: Empty program.
  Setup: An empty list of statements.
  Check: The backend generates a valid C file containing an empty `main` function that compiles with `gcc -Wall -Werror` and returns 0.

- [T6] EDGE_CASE: Loop with zero iterations.
  Setup: A `while (false)` loop or a `for` loop like `for (int i=5; i<5; i++)` containing a print statement.
  Check: The compiled program executes without printing anything from the loop body.

- [T7] EDGE_CASE: Deeply nested control flow.
  Setup: A program with a `for` loop containing an `if` statement which in turn contains another `while` loop.
  Check: The program compiles and executes with the correct, predictable logic and output, verifying correct scoping and brace generation.

- [T8] EDGE_CASE: Correct parenthesization of complex arithmetic expressions.
  Setup: A program that computes and prints `(a + b) * c` and `a + (b * c)` with specific values for `a, b, c` to ensure operator precedence is handled correctly.
  Check: The compiled program prints the two distinct, correct results.

- [T9] EDGE_CASE: Correct parenthesization of array access with a complex index expression.
  Setup: A program with an array `arr` that accesses `arr[(a + b) * 2]`.
  Check: The generated C code correctly parenthesizes the index expression as `arr[(a + b) * 2]` and the program compiles and runs correctly.

- [T10] EDGE_CASE: Identifier sanitization.
  Setup: A program that declares variables with names that are C keywords or contain invalid characters (e.g., `int`, `for`, `my-var`).
  Check: The generated C code uses sanitized names (e.g., `int_`, `for_`, `my_var`), compiles successfully, and runs correctly.

- [T11] STRESS: Large number of sequential statements.
  Setup: A program consisting of over 500 simple assignment statements.
  Check: The C backend generates the code, `gcc` compiles it without error, and the final state of variables is correct. This helps satisfy the "Verified statements >= 330" rubric item.

- [T12] REGRESSION: Signed integer division semantics.
  Setup: A program that computes and prints `-10 / 3`.
  Check: The output of the compiled C program must match the defined semantics of the TrustLean IR for integer division, which may differ from C's default behavior (e.g., rounding towards zero vs. floor).

- [T13] REGRESSION: Boolean expression short-circuiting.
  Setup: A program with a conditional `if (x != 0 && y / x > 1)` where `x` is initialized to 0. The right-hand side would cause a division-by-zero error.
  Check: The program must compile and run without crashing, demonstrating that the C `&&` operator's short-circuiting behavior is correctly leveraged.

- [T14] COVERAGE: All `Stmt` constructors are handled.
  Setup: A single, comprehensive program that utilizes every constructor of the `Stmt` type (e.g., `Declare`, `Assign`, `If`, `While`, `For`, `Store`, `Return`, `Block`, etc.).
  Check: The `stmtToC` function does not fail, and the resulting C code compiles with `gcc -Wall -Werror` and produces a predictable output, confirming all code generation paths are implemented.

- [T15] QUALITY: Automatic inclusion of `stdint.h`.
  Setup: A program that declares a variable of a fixed-width integer type from the IR (e.g., `uint32_t`).
  Check: The generated C code contains the line `#include <stdint.h>` and compiles correctly.

- [T16] QUALITY: Automatic inclusion of `stdbool.h`.
  Setup: A program that declares and uses a `bool` variable.
  Check: The generated C code contains the line `#include <stdbool.h>` and compiles correctly.

---

### N9.5 — Zero Sorry + Final Audit

- **Tipo**: HOJA
- **Archivos fuente**: 
- **Target properties**: `Tests/Properties/ZeroSorry+FinalAudit.lean`
- **Target integration**: `Tests/Integration/ZeroSorry+FinalAudit.lean`
- **Properties**: 6 (4 P0)
- **Integration tests**: 10

PROPERTIES:
- [P1] P0 IDEMPOTENCY: Applying `sanitizeIdentifier` twice to any string should produce the same result as applying it once. This is explicitly required by the rubric.
  Sketch: `property (s : String) : sanitizeIdentifier (sanitizeIdentifier s) = sanitizeIdentifier s`
  SampleableExt: no
  Risk: If not idempotent, repeated sanitization in a pipeline could corrupt identifiers (e.g., `_foo` becomes `__foo`), leading to compilation or linking errors.

- [P2] P0 INVARIANT: The output of `sanitizeIdentifier` must always be a valid C identifier. A valid C identifier starts with a letter or underscore, and contains only letters, digits, or underscores.
  Sketch: `property (s : String) : isValidCIdentifier (sanitizeIdentifier s) = true`
  SampleableExt: no
  Risk: Generation of syntactically incorrect C code that will fail to compile.

- [P3] P0 INVARIANT: The output of `sanitizeIdentifier` must not be a reserved C keyword (e.g., "if", "while", "int", "return").
  Sketch: `property (s : String) : (sanitizeIdentifier s) ∉ cKeywords`
  SampleableExt: no
  Risk: Generation of C code with syntax errors or, worse, unintended semantics, leading to compilation failure or logical bugs.

- [P4] P0 INVARIANT: The C code string generated by `stmtToC` for any statement must have a balanced number of opening and closing curly braces.
  Sketch: `property (stmt : Stmt) : hasBalancedBraces (stmtToC stmt) = true`
  SampleableExt: yes
  Risk: Unbalanced braces are a fatal syntax error, making the entire generated C code file uncompilable.

- [P5] P1 SOUNDNESS: Calling `stmtToC` twice on the exact same `Stmt` value must produce the exact same C code string. This ensures the function is pure and deterministic.
  Sketch: `property (stmt : Stmt) : stmtToC stmt = stmtToC stmt`
  SampleableExt: yes
  Risk: Non-determinism suggests hidden state or side effects, which can lead to unreproducible builds and subtle, hard-to-diagnose bugs in the generated code.

- [P6] P1 PRESERVATION: If a statement `s1` contains a sub-statement `s2` (e.g., the body of a loop or an if-branch), the C code generated for `s1` must contain the C code for `s2` as a substring.
  Sketch: `property (s1 : Stmt) : ∀ (s2 ∈ s1.subStmts), (stmtToC s2).isSubstringOf (stmtToC s1)`
  SampleableExt: yes
  Risk: The code generator might silently drop or corrupt entire blocks of the program logic (e.g., the body of a loop), leading to severe, incorrect behavior.

INTEGRATION:
- [T1] BASIC: A simple sequence of a variable declaration and an assignment within a block.
  Setup: Construct a `Stmt` representing a block with two statements: `let x = 5;` followed by `y = x;`.
  Check: The resulting string should be semantically equivalent to `{\n  int x = 5;\n  y = x;\n}`. Exact formatting may vary, but tokens and structure must be correct.

- [T2] EDGE_CASE: `sanitizeIdentifier` correctly handles a C keyword.
  Setup: Construct a `Stmt` that declares a variable whose name is a C keyword, e.g., `let while = 1;`.
  Check: The output C code must use a sanitized name, e.g., `int while_ = 1;` or `int _while = 1;`, and must not be `int while = 1;`.

- [T3] EDGE_CASE: `sanitizeIdentifier` correctly handles a string that is not a valid C identifier (e.g., starts with a digit).
  Setup: Construct a `Stmt` that declares a variable with a name like `"99problems"`.
  Check: The output C code must use a sanitized name, e.g., `int _99problems = 0;`.

- [T4] BASIC: Nested control flow statements to check for correct brace placement and structure.
  Setup: Construct a `Stmt` for an if-else-if structure, e.g., `if(c1) { s1 } else { if(c2) { s2 } }`.
  Check: The output C code must have correctly nested blocks with balanced braces, e.g., `if (c1) { ... } else { if (c2) { ... } }`.

- [T5] EDGE_CASE: Code generation for an empty block of statements.
  Setup: Construct a `Stmt.block` with an empty list of statements.
  Check: The output should be a valid empty block, like `{}` or `{ }`, and the generator should not crash.

- [T6] EDGE_CASE: `store`/`load` base expression parenthesization. This directly tests a rubric criterion.
  Setup: Construct a `Stmt` representing a memory store where the base address is a complex expression, e.g., `(p + q)[0] = 42;`.
  Check: The output C code must correctly parenthesize the base expression `(p + q)` to respect C operator precedence. The result must be `(p + q)[0] = 42;` and NOT `p + q[0] = 42;`.

- [T7] BASIC: Coverage of all 12 `Stmt` constructors.
  Setup: Create a list of 12 simple but distinct `Stmt` instances, one for each constructor defined in the AST.
  Check: For each of the 12 statements, `stmtToC` produces a syntactically plausible C code snippet. This is a smoke test to ensure no constructor was forgotten and causes a crash.

- [T8] STRESS: Deeply nested statements to test for potential stack overflow or performance degradation in the recursive generator.
  Setup: Programmatically construct a `Stmt` with 100 levels of nested `if` statements or `block`s.
  Check: The `#eval` completes in a reasonable time without crashing. The output string should be syntactically valid with 100 pairs of correctly nested braces.

- [T9] REGRESSION: An `if` statement without an `else` branch is handled correctly.
  Setup: Construct an `if` statement that has a `then` branch but no `else` branch (assuming the AST supports this, e.g., with an `Option Stmt`).
  Check: The generated C code should be a simple `if (condition) { ... }` block, with no `else` keyword or empty `else` block.

- [T10] BASIC: Top-level file generation includes required headers.
  Setup: Use the top-level C-file-generating function (e.g., `astToCFile`) on a simple list of statements.
  Check: The output string must begin with the required includes as specified by the rubric: `#include <stdint.h>\n#include <stdbool.h>\n`.

---

## Resumen

| Métrica | Total |
|---------|-------|
| Nodos | 13 |
| Properties | 74 (34 P0) |
| Integration tests | 126 |
| Archivos .lean a crear | 26 |
